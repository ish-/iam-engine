#pragma once

class WCallbacks;
class WGraphics;
class WInputs;
class WPhysics;
class WTime;

struct WContext
{
  // WCallbacks &callbacks;

  // WGraphics &graphics;
  // WInputs &inputs;
  // WPhysics &physics;
  WTime &time;
};
